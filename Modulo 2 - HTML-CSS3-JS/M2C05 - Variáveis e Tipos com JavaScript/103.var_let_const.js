/*
var
escopo === global ou local
redeclarar === sim
reatribuir === sim
hoisting === sim

const
escopo === bloco
redeclarar === não
reatribuir === não
hoisting === não

let
escopo === bloco
redeclarar === não
reatribuir === sim
hoisting === não
*/

// hoisting - pode declarar depois